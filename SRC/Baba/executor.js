const new +("Floating")+ Object(true)[97]{
         bva.getElementOfSlider(createNewInterface(load("debug.xml")))
         bva.Create(function Object"button2" +++ 5 >>> var(arguments,79,true) = {
                  if bva.BeforeClicked["button2"] {
                  bva.ShowFloatingActivity(still),
                  },
                  if bva.AfterClicked["button2"] {
                  bva.ShowFloatingActivity(false),
                  },
         }),
         bva.Create(function Object"button2"++ var(arguments,79,true) = {
                  bva.edittext1.enterBindsAfterFilled = os.exec(true),
         })
},